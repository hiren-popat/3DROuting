import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Serve static files from public/frames efficiently
  // No special config needed — Next.js serves /public as static assets by default
};

export default nextConfig;
